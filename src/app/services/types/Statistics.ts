export interface GeneralStats {
  user_count: number | string,
  guild_count: number | string,

  // some general stats about the bot modules
  giveaway_count: number | string,
  ticket_count: number | string,
  punish_count: number | string,
  global_verified_count: number | string,
}
