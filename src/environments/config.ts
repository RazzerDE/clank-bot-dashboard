export const config = {
  api_url = 'http://localhost:4200',  // used for general api calls
  redirect_url = 'http://localhost:4200/authenticate',  // used for the discord oauth2
  client_id = 'YOUR_DISCORD_CLIENT_ID'  // used for the discord oauth2
}
